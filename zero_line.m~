function zero_line(varargin)
yval = 0;
if nargin == 1
    yval = varargin{1};
end

hold on;
xs = get(gca, 'XLim');
plot(xs, [yval, yval], '-', 'Color', [0.5, 0.5, 0.5]);
set(gca, 'XLim', xs);
return